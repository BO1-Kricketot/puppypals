import { StyleSheet, Text, SafeAreaView, View } from 'react-native';

export default function Profile() {
  return (
    <SafeAreaView>
      <Text>Look, it's a Profile!</Text>
    </SafeAreaView>
  );
};